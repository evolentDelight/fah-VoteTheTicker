{
  "name": "votetheticker",
  "version": "1.0.0",
  "description": "VoteTheTicker — Verified-human club mini app for stock candidate discovery and democratic voting",
  "private": true,
  "scripts": {
    "dev": "npx concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "dev:server": "cd server && npm run dev",
    "dev:client": "cd client && npm run dev",
    "build": "cd client && npm install && npm run build",
    "start": "cd server && node index.js",
    "render:build": "npm install && cd server && npm install && cd ../client && npm install && npm run build",
    "render:start": "cd server && node index.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
